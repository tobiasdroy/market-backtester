import { describe, expect, it } from 'vitest'

// Placeholder smoke test confirming the Vitest toolchain is wired up.
// Replace/extend with real engine tests in Phase 2 (simulate.ts, stats.ts, etc).
describe('project scaffolding', () => {
  it('runs tests', () => {
    expect(1 + 1).toBe(2)
  })
})
